import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const BuyStock = () => {
  return (
    <View>
      <Text>This is buy stock screen</Text>
    </View>
  );
};

export default BuyStock;

const styles = StyleSheet.create({});
