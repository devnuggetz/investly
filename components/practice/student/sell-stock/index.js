import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SellStock = () => {
  return (
    <View>
      <Text>This is sell stock screen</Text>
    </View>
  );
};

export default SellStock;

const styles = StyleSheet.create({});
